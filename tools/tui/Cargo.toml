[package]
name = "lightwallet-tui"
description = "lwtui: a live terminal monitor for Zcash lightwallet indexers"
version.workspace = true
edition.workspace = true
publish.workspace = true

[[bin]]
name = "lwtui"
path = "src/main.rs"

[dependencies]
# Both variants on: the monitor points at either surface at runtime via
# --variant, the same stance lwcli takes.
lightwallet-core = { path = "../../crates/core", features = ["canonical", "crosslink"] }
# The shared transaction projection: lwtui and lwcli parse
# through it identically. This is where the zcash_primitives weight lives now.
lightwallet-txview = { path = "../txview" }
anyhow.workspace = true
clap.workspace = true
crossterm.workspace = true
futures-util.workspace = true
ratatui.workspace = true
serde = { workspace = true }
serde_json.workspace = true
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "sync", "time"] }
tonic = { workspace = true, features = ["tls-ring", "tls-webpki-roots"] }
